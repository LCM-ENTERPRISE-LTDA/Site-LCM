/**
 * @deprecated Milestone 4 — boxed TriadScene removed from Home.
 * Geometry now lives in HeroEnvironment (full-bleed immersive field).
 * File retained as legacy reference only; not imported by Home.
 */
export function TriadScene() {
  return null;
}
